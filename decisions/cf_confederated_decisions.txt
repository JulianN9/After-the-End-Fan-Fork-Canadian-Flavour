title_decisions = {
	secretly_convert_to_united_holy_site = {
		filter = owned
		ai_target_filter = self
	
		only_playable = yes
		
		from_potential = {
			ai = no
			NOT = { is_incapable = yes }
			NOT = { secret_religion = united }
			NOT = { religion = united }
			NOT = { controls_religion = yes }
		}
		
		potential = {
			tier = COUNT
			NOT = {
				location = { religion = united }
			}
			OR = {
				is_holy_site = united
				any_de_jure_vassal_title = {
					is_holy_site = united
				}
			}
			OR = {
				holder_scope = {
					character = FROM
				}
				AND = {
					tier = BARON
					holder_scope = {
						liege = {
							character = FROM
						}
					}
				}
			}
			has_global_flag = canadian_church_unified
		}
		
		allow = {
			FROM = {
				piety = 250
				prisoner = no
				NOT = { is_incapable = yes }
				NOT = { is_inaccessible_trigger = yes }
				NOT = { society_member_of = secret_religious_cult }
			}
		}
		
		effect = {
			FROM = {
				piety = -250
				set_secret_religion = united
			}
		}
		
		revoke_allowed = {
			always = no
		}
		
		ai_will_do = {
			factor = 0
		}
	}
}